# compiler/runtime/Cargo.toml

[package]
name = "runtime"
version = "0.1.0"
edition = "2021"

[lib]
path = "mod.rs"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
toml = "0.8"

# Internal dependencies
ir = { path = "../ir" }
dsl-compiler = { path = "../../dsl" }
